name: Django Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_USER: online_shopping
          POSTGRES_PASSWORD: halawaelhagag13
          POSTGRES_DB: test_online_fashion_store_db
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

<<<<<<< HEAD
- name: Install Dependencies
run: |
  python -m pip install --upgrade pip
  pip install -r requirements.txt

- name: Run Tests
env:
  DATABASE_URL: postgres://online_shopping:halawaelhagag13@localhost:5432/test_online_fashion_store_db
run: python manage.py test
>>>>>>> 6d20291a9707503536f021bee6dc984e27f46865

    - name: Run Tests
      env:
        DATABASE_URL: postgres://online_shopping:halawaelhagag13@localhost:5432/test_online_fashion_store_db
      run: python manage.py test